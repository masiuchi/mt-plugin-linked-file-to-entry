package LinkedFileToEntry::L10N::ja;
use strict;
use warnings;
use base qw( LinkedFileToEntry::L10N );

our %Lexicon = (

    # config.yaml
    'Link to file for entries.' =>
        '記事／ページの編集画面に「ファイルへのリンク（本文）」「ファイルへのリンク（続き）」を追加します。',

    # LinkToFileEntry::Callback
    'Link to File (text)'      => 'ファイルへのリンク（本文）',
    'Link to File (text_more)' => 'ファイルへのリンク（続き）',
);

1;
